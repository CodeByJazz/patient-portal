<% content_for :header_title do %>
  <h2>Visit History</h2>
<% end %>
<a href="/<%=@name%>/add_visit">Add New Visit</a>
<ul>
  <% sort_by_date(@visits).each do |date| %>
  <li>
    <a href="/<%= @name %>/<%= date.gsub("-", "_") %>/visit_summary"><%= format(date) %></a>
  </li>
  <% end %>
</ul>
